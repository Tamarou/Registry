---
name: Admin Dashboard
slug: admin-dashboard
description: |
  Single-step admin dashboard. Loads overview stats, program data,
  events, waitlist, and notifications. Filter actions use stay
  semantics. Admin tools (drop/transfer approval, template editor,
  program creation) launch via callcc continuations.

steps:
  - slug: dashboard-overview
    description: Admin dashboard with sections and callcc into admin tools
    class: Registry::DAO::WorkflowSteps::AdminDashboardOverview
